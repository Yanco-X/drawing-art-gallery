Flask>=3.1
SQLAlchemy>=2.0.36
alembic>=1.14
# psycopg 3, not psycopg2-binary: psycopg2 has no wheels for Python 3.14
# and would need a local C toolchain to build.
psycopg[binary]>=3.2.10
python-dotenv>=1.0
# Owner sessions. Werkzeug ships with Flask and does the password hashing;
# Flask-Login only carries the user id in the signed session cookie.
flask-login>=0.6.3
# Image decoding, EXIF handling, resizing, WebP encoding.
Pillow>=11.0
# S3-compatible object storage. Works against AWS S3, Cloudflare R2,
# Backblaze, and MinIO -- only the endpoint and credentials differ.
boto3>=1.35
